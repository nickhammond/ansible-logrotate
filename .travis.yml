---
sudo: required
language: python
python: "2.7"
services:
  - docker
before_install:
  #- sudo apt-get update -qq
  #- sudo apt-get install -o Dpkg::Options::="--force-confold" --force-yes -y docker-engine
  - docker pull retr0h/molecule:latest
  - docker run retr0h/molecule:latest
install:
  - pip install ansible
script:
  - molecule test
notifications:
  email: false
